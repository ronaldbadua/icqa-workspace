/** Inclusive range of hours (0–23) shown on Hourly Notes. */
export const HOURLY_NOTES_HOUR_START = 6;
export const HOURLY_NOTES_HOUR_END = 18;

/**
 * Virtual hour value for the second "Stand Up" row (inserted between 9 AM and 10 AM).
 * Uses a value outside the 0–23 range so it never conflicts with real clock hours.
 */
export const STAND_UP_2_HOUR = 95;
